# Optional: the app automatically falls back to the browser's Dutch voice.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
# Optional: a Tamil voice for the explanations ("Uitleg: தமிழ்"). Clips are
# pre-generated with `pnpm speech:generate`; the key is not needed at runtime.
ELEVENLABS_VOICE_ID_TA=
